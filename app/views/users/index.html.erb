<% @users.each do | user | %>
<div class="center">
<ul>
<h3>
<li>  <%= link_to ("#{user.name} has made  #{user.user_languages.count} entries!"), user_user_languages_path(user) %> </li> 
<h3>
</ul>
</div>
<% end %>